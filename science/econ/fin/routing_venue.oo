// # Financial Venue Analytics & Selection
//
// Logline: Destubbed onto ./routing_types.oo Int-floor API.
//
// Setup: Companion types destubbed; keep fail-closed real calls.
//
// Beats:
//   1. Import destubbed companion.
//   2. Wrap constructor and Int query.
//   3. Deterministic work helper for prove.

import "./routing_types.oo";

pub fn routing_venue_init(side: Int, total_qty: Int, limit_price: Int) -> OrderRoute {
    return order_route_new(side, total_qty, limit_price);
}
