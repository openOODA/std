// # LLVM IR Textual Emitter
//
// Logline: Destubbed onto ./llvm_ir_types.oo Int-floor API.
//
// Setup: Companion types destubbed; keep fail-closed real calls.
//
// Beats:
//   1. Import destubbed companion.
//   2. Wrap constructor and Int query.
//   3. Deterministic work helper for prove.

import "./llvm_ir_types.oo";

pub fn llvm_ir_emitter_init(kind: Int, bit_width: Int, is_signed: Int) -> LLVMType {
    return llvm_type_new(kind, bit_width, is_signed);
}
