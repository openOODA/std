// # Result println refuse
//
// Logline: emit-llvm fail-closes println of Result[Int] instead of oo_print_str.
//
// Setup: Backend-LLVM. Not a run lock.
//
// Beats:
//   1. Bind r as Result[Int, String] Ok(7).
//   2. println(r).
//   3. Emit must print ERR llvm_emit.

pub fn verify_llvm_println_resi() -> Bool {
    let r: Result[Int, String] = Ok(7);
    println(r);
    return true;
}
