// # Typestate Callee Ctor Only Bad
//
// Logline: Standard library verification and domain capabilities for Typestate Callee Ctor Only Bad.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

fn open_sock() -> Int { return 1; }

fn wrap(s: Int) {
    let t = open_sock();
}

pub fn verify_typestate_callee_ctor_only_bad() -> Bool {
    let s = open_sock();
    wrap(s);
    return true;
}
