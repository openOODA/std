// # Parse a TYPESTATE fn without an arrow
//
// Logline: Declare send as Open with no arrow so the TYPESTATE parser must refuse.
//
// Setup: Test fixture. Keep TYPESTATE directives and all code exact.
//
// Beats:
//   1. Declare Sock Closed Open.
//   2. Write TYPESTATE fn send: Open with no arrow.
//   3. Call send from main.
//
// TYPESTATE: Sock Closed Open
// TYPESTATE fn send: Open

fn send(s: Int) { let x = s; }

pub fn verify_typestate_fn_no_arrow() -> Bool {
    send(1);
    return true;
}
