// # Typestate Cross Ok
//
// Logline: Standard library verification and domain capabilities for Typestate Cross Ok.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

import "typestate_lib.oo";

pub fn verify_typestate_cross_ok() -> Bool {
    let s = open_sock();
    connect(s);
    send(s);
    return true;
}
