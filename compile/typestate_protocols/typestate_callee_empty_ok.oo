// # Typestate Callee Empty Ok
//
// Logline: Standard library verification and domain capabilities for Typestate Callee Empty Ok.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

fn open_sock() -> Int { return 1; }
fn connect(s: Int) { }
fn send(s: Int) { let x = s; }

pub fn verify_typestate_callee_empty_ok() -> Bool {
    let s = open_sock();
    connect(s);
    send(s);
    return true;
}
