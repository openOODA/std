// # Mark typestate residual
//
// Logline: Print 0 as a named TYPE_STATE residual marker with no enforcement.
//
// Setup: Test fixture. Keep the TYPE_STATE tag and all code exact.
//
// Beats:
//   1. Carry the TYPE_STATE residual tag.
//   2. Name the surface only.
//   3. Print 0 from main.
//
// Residual marker fixture — see bootstrap/TYPE_STATE.oot
// TYPE_STATE: residual
// Named surface only. Not enforced.
pub fn verify_typestate_marker() -> Bool {
    println(0);
    return true;
}
