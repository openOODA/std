// # Send after a mixed if
//
// Logline: Connect only in the true arm so send after the join must fail.
//
// Setup: Test fixture. Keep TYPESTATE directives and all code exact.
//
// Beats:
//   1. Open into Closed.
//   2. Connect only in the true arm.
//   3. Send after the mixed join.
//
// TYPESTATE: Sock Closed Open
// TYPESTATE fn open_sock: -> Closed
// TYPESTATE fn connect: Closed -> Open
// TYPESTATE fn send: Open -> Open

fn open_sock() -> Int { return 1; }
fn connect(s: Int) { let x = s; }
fn send(s: Int) { let x = s; }

pub fn verify_typestate_if_join_bad() -> Bool {
    let s = open_sock();
    if true {
        connect(s);
    } else {
        let z = 0;
    }
    send(s);
    return true;
}
