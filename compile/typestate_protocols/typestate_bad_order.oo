// # Send before connect
//
// Logline: Send on a Closed Sock before connect so TYPESTATE must refuse the order.
//
// Setup: Test fixture. Keep TYPESTATE directives and all code exact.
//
// Beats:
//   1. Open into Closed.
//   2. Call send while still Closed.
//   3. Connect after the illegal send.
//
// TYPESTATE: Sock Closed Open
// TYPESTATE fn open_sock: -> Closed
// TYPESTATE fn connect: Closed -> Open
// TYPESTATE fn send: Open -> Open

fn open_sock() -> Int { return 1; }
fn connect(s: Int) { let x = s; }
fn send(s: Int) { let x = s; }

pub fn verify_typestate_bad_order() -> Bool {
    let s = open_sock();
    send(s);
    connect(s);
    return true;
}
