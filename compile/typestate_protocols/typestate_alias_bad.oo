// # Send a Closed alias
//
// Logline: Alias t = s and send t while the Sock is still Closed.
//
// Setup: Test fixture. Keep TYPESTATE directives and all code exact.
//
// Beats:
//   1. Open into Closed.
//   2. Bind t = s.
//   3. Send t with no connect.
//
// TYPESTATE: Sock Closed Open
// TYPESTATE fn open_sock: -> Closed
// TYPESTATE fn send: Open -> Open

fn open_sock() -> Int { return 1; }
fn send(s: Int) { let x = s; }

pub fn verify_typestate_alias_bad() -> Bool {
    let s = open_sock();
    let t = s;
    send(t);
    return true;
}
