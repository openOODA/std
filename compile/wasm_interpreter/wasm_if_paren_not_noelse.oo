// # Const if (!false) println no else
//
// Logline: Print pnot-ne when (!false) and there is no else branch.
//
// Setup: No imports. One public main.
//
// Beats:
//   1. Declare public main.
//   2. If (!false) print pnot-ne.
//   3. Leave no else and no other statements.

pub fn verify_wasm_if_paren_not_noelse() -> Bool {
    if (!false) {
        println("pnot-ne");
    }
    return true;
}
