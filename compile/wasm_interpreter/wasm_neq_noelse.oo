// # Const if 1 != 2 println no else
//
// Logline: Print neq-ne when 1 != 2 and there is no else branch.
//
// Setup: No imports. One public main.
//
// Beats:
//   1. Declare public main.
//   2. If 1 != 2 print neq-ne.
//   3. Leave no else and no other statements.

pub fn verify_wasm_neq_noelse() -> Bool {
    if 1 != 2 {
        println("neq-ne");
    }
    return true;
}
