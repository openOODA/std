// # Hostile ident div fold
//
// Logline: Print 8 / x so emit-wasm must miss a const 8/2 fold.
//
// Setup: One public main. Right operand is IDENT x, not INT.
//
// Beats:
//   1. Let x be 2.
//   2. Print 8 / x.
//   3. A 4-only module is a lie.

pub fn verify_wasm_div_hostile() -> Bool {
    let x = 2;
    println(8 / x);
    return true;
}
