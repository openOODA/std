// # Hostile ident sub fold
//
// Logline: Print 9 - x so emit-wasm must miss a const 9-4 fold.
//
// Setup: One public main. Right operand is IDENT x, not INT.
//
// Beats:
//   1. Let x be 4.
//   2. Print 9 - x.
//   3. A 5-only module is a lie.

pub fn verify_wasm_sub_hostile() -> Bool {
    let x = 4;
    println(9 - x);
    return true;
}
