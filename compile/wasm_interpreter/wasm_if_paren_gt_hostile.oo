// # Hostile if (2 > x) fold
//
// Logline: Print fold-me or other from if (2 > x) so emit-wasm must miss a const-paren-gt fold.
//
// Setup: One public main. Right operand is IDENT x.
//
// Beats:
//   1. Let x be 1.
//   2. If (2 > x) print fold-me else print other.
//   3. A fold-me-only module is a lie.

pub fn verify_wasm_if_paren_gt_hostile() -> Bool {
    let x = 1;
    if (2 > x) {
        println("fold-me");
    } else {
        println("other");
    }
    return true;
}
