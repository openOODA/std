// # Hostile ident unary-minus fold
//
// Logline: Print -x so emit-wasm must miss a const -8 fold.
//
// Setup: One public main. Operand is IDENT x, not INT.
//
// Beats:
//   1. Let x be 8.
//   2. Print -x.
//   3. A -8-only module is a lie.

pub fn verify_wasm_neg_hostile() -> Bool {
    let x = 8;
    println(-x);
    return true;
}
