// # Hostile ident add fold
//
// Logline: Print 1 + x so emit-wasm must miss a const 1+2 fold.
//
// Setup: One public main. Right operand is IDENT x, not INT.
//
// Beats:
//   1. Let x be 2.
//   2. Print 1 + x.
//   3. A 3-only module is a lie.

pub fn verify_wasm_add_hostile() -> Bool {
    let x = 2;
    println(1 + x);
    return true;
}
