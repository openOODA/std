// # Hostile if 1 == x fold
//
// Logline: Print fold-me or other from if 1 == x so emit-wasm must miss a const-eq fold.
//
// Setup: One public main. Right operand is IDENT x, not INT.
//
// Beats:
//   1. Let x be 1.
//   2. If 1 == x print fold-me else print other.
//   3. A fold-me-only module is a lie.

pub fn verify_wasm_if_eq_hostile() -> Bool {
    let x = 1;
    if 1 == x {
        println("fold-me");
    } else {
        println("other");
    }
    return true;
}
