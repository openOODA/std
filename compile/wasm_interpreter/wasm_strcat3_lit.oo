// # Println strcat of three string lits
//
// Logline: Print Hello, n-join! from three literals so emit-wasm must n-join then fd_write.
//
// Setup: No imports. One public main. The body is only that println concat.
//
// Beats:
//   1. Declare public main.
//   2. Print "Hello, " plus "n" plus "-join!".
//   3. Leave no other statements.

pub fn verify_wasm_strcat3_lit() -> Bool { println("Hello, " + "n" + "-join!");     return true;
}
