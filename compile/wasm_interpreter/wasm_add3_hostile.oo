// # Hostile ident three-add fold
//
// Logline: Print 1 + 2 + x so emit-wasm must miss a const 1+2+3 fold.
//
// Setup: One public main. Third operand is IDENT x, not INT.
//
// Beats:
//   1. Let x be 3.
//   2. Print 1 + 2 + x.
//   3. A 6-only module is a lie.

pub fn verify_wasm_add3_hostile() -> Bool {
    let x = 3;
    println(1 + 2 + x);
    return true;
}
