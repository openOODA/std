// # Triple string strcat print
//
// Logline: Print Hello, strcat!extra from three literals so emit-wasm must join then fd_write.
//
// Setup: No imports. One public main. The body is only that println concat.
//
// Beats:
//   1. Declare public main.
//   2. Print "Hello, " plus "strcat!" plus "extra".
//   3. Leave no other statements.
//
// residual: IDENT in the plus chain is not this fixture.

pub fn verify_wasm_strcat_hostile() -> Bool { println("Hello, " + "strcat!" + "extra");     return true;
}
