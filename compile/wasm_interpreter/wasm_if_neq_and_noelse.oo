// # Const if 1!=2 && 3!=4 println no else
//
// Logline: Print neqand-ne when 1!=2 && 3!=4 and there is no else branch.
//
// Setup: No imports. One public main.
//
// Beats:
//   1. Declare public main.
//   2. If 1!=2 && 3!=4 print neqand-ne.
//   3. Leave no else and no other statements.

pub fn verify_wasm_if_neq_and_noelse() -> Bool {
    if 1 != 2 && 3 != 4 {
        println("neqand-ne");
    }
    return true;
}
