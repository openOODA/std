// # GOOSE Fail-Closed Multicast Stubs
//
// Logline: CAP-bound stubs for L2/IGMP multicast join and IEC 61850-9-3 Ed25519/RSA signature verify that fail-closed via fail-closed error.
//
// Setup: live multicast transport is process_exit in stubs. CAP-G6: GOOSE is ethertype 0x88B8 L2 multicast, no SoftGranular.
//
// Beats:
//   1. goose_join_multicast: NetCap-bound L2 multicast join stub.
//   2. goose_verify_signature: CryptoCap-bound Ed25519/RSA verify stub.

pub fn goose_join_multicast(net: &NetCap, mac: String) -> Result[Int, String] {
    return Err("Unimplemented: GOOSE multicast join requires NetCap raw residual (no SoftGranular) FFI lowering");
}

pub fn goose_verify_signature(crypto: &CryptoCap, frame: &GooseFrame, sig: &GooseSignature) -> Result[Bool, String] {
    return Err("Unimplemented: GOOSE signature verify requires CryptoCap FFI lowering");
}
