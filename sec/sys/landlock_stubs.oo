// # Landlock Fail-Closed Stubs
//
// Logline: Fail-closed process_exit residuals for landlock_create_ruleset, add_rule, restrict_self, and get_rule.
//
// Setup: Imports landlock_engine.oo. RULES.oot 1.4 default-deny; RULES.oot 1.3 fail-closed residual.
//
// Beats:
//   1. landlock_create_ruleset_sys: refuse sys_landlock_create_ruleset syscall.
//   2. landlock_add_rule_path_beneath_sys: refuse LANDLOCK_RULE_PATH_BENEATH add.
//   3. landlock_add_rule_net_port_sys: refuse LANDLOCK_RULE_NET_PORT add.
//   4. landlock_restrict_self_sys: refuse sys_landlock_restrict_self.
//
// residual: not a real landlock driver; integrate host kernel landlock.

// RULES.oot 1.4: Default-deny for privileged effects. Explicit &SysCap required.
// RULES.oot 1.3: Fail-closed residual for unimplemented builtins.

import "std/sec/sys/landlock_engine.oo";

pub fn landlock_create_ruleset_sys(sys: &SysCap, ruleset: &LandlockRuleset) -> Result[Int, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for sys_landlock_create_ruleset syscall.
    // Sandbox ruleset creation and enforcement at kernel layer must be performed by host runtime.
    return Err("Unimplemented: kernel landlock_create_ruleset requires native host FFI lowering");
}

pub fn landlock_add_rule_path_beneath_sys(
    sys: &SysCap,
    ruleset_fd: Int,
    path_rule: &PathBeneathAttr
) -> Result[Bool, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for sys_landlock_add_rule (LANDLOCK_RULE_PATH_BENEATH) syscall.
    // File descriptor and inode path restriction application requires native kernel FFI bindings.
    return Err("Unimplemented: kernel landlock_add_rule path beneath requires native host FFI lowering");
}

pub fn landlock_add_rule_net_port_sys(
    sys: &SysCap,
    ruleset_fd: Int,
    net_rule: &NetPortAttr
) -> Result[Bool, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for sys_landlock_add_rule (LANDLOCK_RULE_NET_PORT) syscall.
    // TCP port landlock restriction application requires native kernel FFI bindings.
    return Err("Unimplemented: kernel landlock_add_rule net port requires native host FFI lowering");
}

pub fn landlock_restrict_self_sys(sys: &SysCap, ruleset_fd: Int, flags: Int) -> Result[Bool, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for sys_landlock_restrict_self syscall.
    // Process domain enforcement requires prctl(PR_SET_NO_NEW_PRIVS) and landlock_restrict_self kernel lowering.
    return Err("Unimplemented: kernel landlock_restrict_self requires native host FFI lowering");
}

pub fn landlock_get_abi_version_sys(sys: &SysCap) -> Result[Int, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for landlock ABI query (LANDLOCK_CREATE_RULESET_VERSION).
    // Landlock LSM kernel version detection must be handled by host platform runtime.
    return Err("Unimplemented: kernel Landlock ABI version query requires native host FFI lowering");
}
