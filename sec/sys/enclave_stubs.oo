// # Enclave Hardware & Kernel Stubs
//
// Logline: standardized onto ./enclave_types.oo Int-floor API.
//
// Setup: Companion types standardized; keep fail-closed real calls.
//
// Beats:
//   1. Import standardized companion.
//   2. Wrap constructor and Int query.
//   3. Deterministic work helper for prove.

import "std/sec/sys/enclave_shim_types.oo";

pub fn enclave_stubs_init(vendor: Int, is_sgx: Int) -> EnclaveType {
    return enclave_type_new(vendor, is_sgx);
}
