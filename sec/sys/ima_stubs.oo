// # IMA Fail-Closed Stubs
//
// Logline: Fail-closed process_exit residuals for securityfs read, policy write, EVM xattr set, and TPM quote.
//
// Setup: Imports ima_engine.oo. RULES.oot 1.4 default-deny; RULES.oot 1.3 fail-closed residual.
//
// Beats:
//   1. ima_read_ascii_runtime_measurements_sys: refuse securityfs /sys/kernel/security/ima read.
//   2. ima_write_policy_sys: refuse /sys/kernel/security/ima/policy write.
//   3. evm_set_file_xattr_sys: refuse setxattr(2) on security.evm.
//   4. fail-closed error: default-deny on unbuilt host FFI surface.
//
// residual: not a real IMA client; integrate host kernel IMA driver.

// RULES.oot 1.4: Default-deny for privileged effects. Explicit &SysCap required.
// RULES.oot 1.3: Fail-closed residual for unimplemented builtins.

import "std/sec/sys/ima_engine.oo";

pub fn ima_read_ascii_runtime_measurements_sys(
    sys: &SysCap,
    path: String
) -> Result[List[ImaMeasurement], String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for reading securityfs /sys/kernel/security/ima/ascii_runtime_measurements.
    // IMA measurement log parsing requires direct kernel securityfs interface lowering.
    return Err("Unimplemented: kernel IMA measurement log reading requires native host FFI lowering");
}

pub fn ima_write_policy_sys(
    sys: &SysCap,
    policy_str: String
) -> Result[Bool, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for writing to /sys/kernel/security/ima/policy.
    // Loading IMA policy rules into the kernel runtime requires privileged FFI lowering.
    return Err("Unimplemented: kernel IMA policy write requires native host FFI lowering");
}

pub fn evm_set_file_xattr_sys(
    sys: &SysCap,
    path: String,
    sig: &EvmSignature
) -> Result[Bool, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for setxattr(2) system call (security.ima / security.evm).
    // File integrity extended attribute modification requires host kernel FFI bindings.
    return Err("Unimplemented: kernel setxattr security.evm requires native host FFI lowering");
}

pub fn evm_verify_file_xattr_sys(
    sys: &SysCap,
    path: String
) -> Result[Int, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for getxattr(2) and kernel EVM appraisal verification.
    // Querying EVM attribute verification state requires native host kernel FFI bindings.
    return Err("Unimplemented: kernel EVM extended attribute verification requires native host FFI lowering");
}

pub fn ima_read_tpm_pcr_sys(
    sys: &SysCap,
    pcr_index: Int
) -> Result[String, String] {
    // COMPILER LIMITATION: openOODA lacks native C-FFI lowering for TPM 2.0 device driver (/dev/tpmrm0) communication.
    // Reading hardware TPM PCR 10 register state requires native host FFI bindings.
    return Err("Unimplemented: kernel TPM PCR reading requires native host FFI lowering");
}

type SysCap = struct {};
type Fail = struct {};
type Default = struct {};


pub fn __dummy_std_src_ooda_sec_sys_ima_stubs_oo() -> Int {
    return 0;
}
