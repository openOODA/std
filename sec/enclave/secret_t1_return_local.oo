// # Return a secret local
//
// Logline: Return a secret-tagged local and print the caller result.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag api_key as secret.
//   2. Return the local from make_secret.
//   3. Print the caller binding.
//
// T1-1 attack: fn returns secret-tagged local via return; caller println(result)
// Expect (product honesty residual): interproc return taint not path-A floor.
// SECRET: api_key
pub fn make_secret() -> String {
    let api_key = "sk-live";
    return api_key;
}
pub fn verify_secret_t1_return_local() -> Bool {
    let y = make_secret();
    println(y);
    return true;
}
