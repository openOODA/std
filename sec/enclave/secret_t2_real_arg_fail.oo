// # Refuse a real secret argument
//
// Logline: Print a secret IDENT so the sink must refuse.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag api_key as secret.
//   2. Bind api_key to a string.
//   3. Print api_key.
//
// T2-3 TP: real SECRET bare IDENT arg — MUST refuse
// SECRET: api_key
pub fn verify_secret_t2_real_arg_fail() -> Bool {
    let api_key = "s3cret";
    println(api_key);
    return false;
}
