// # Refuse secret concat in println
//
// Logline: Print a literal concatenated with a secret IDENT so the sink must refuse.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag token as secret.
//   2. Concat a public literal with token.
//   3. Print the concat.
//
// T2 peer1 args: println("literal " + secret) IDENT in concat must refuse
// SECRET: token
pub fn verify_secret_t2_args_println_concat_fail() -> Bool {
    let token = "s3cret";
    println("literal " + token);
    return false;
}
