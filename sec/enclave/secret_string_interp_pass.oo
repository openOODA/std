// # Print a clean concat
//
// Logline: Concat two public literals and print the result while a secret name sits unused.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag token as secret.
//   2. Build msg from two public literals.
//   3. Print msg, never the secret.
//
// M-? string-interp clean: concat with no secret identifier reaches sink
// SECRET: token
pub fn verify_secret_string_interp_pass() -> Bool {
    let token = "s3cret";
    let msg = "ok " + "world";
    println(msg);
    return true;
}
