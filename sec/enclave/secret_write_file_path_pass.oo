// # Allow a public write_file path
//
// Logline: Write public content to a public path while a secret name sits unused.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag token as secret.
//   2. Call write_file with a public path and content.
//   3. Print 1 after the call.
//
// Secret allow non-secret: write_file_path
// SECRET: token
pub fn verify_secret_write_file_path_pass(fs: &FsWriteCap) -> Bool {
    let token = "/tmp/sec";
    let path = "/tmp/ok";
    let content = "ok";
    let r = write_file(fs, path, content);
    println(1);
    return true;
}
