// # Print a header that mentions the name
//
// Logline: Print a longer public string that contains the secret spelling.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag api_key as secret.
//   2. Bind a header that mentions api_key as text.
//   3. Print the header.
//
// T2-1c FP: longer string containing secret spelling — must NOT refuse
// SECRET: api_key
pub fn verify_secret_t2_string_contains_name_fp() -> Bool {
    let hdr = "X-Header: api_key placeholder";
    println(hdr);
    return true;
}
