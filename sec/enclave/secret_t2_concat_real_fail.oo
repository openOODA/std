// # Refuse concat that carries a secret
//
// Logline: Concat a public prefix with a secret IDENT and print the result.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag api_key as secret.
//   2. Bind msg to "k=" + api_key.
//   3. Print msg.
//
// T2-3b TP: secret IDENT in concat reaches println — must refuse (prop)
// SECRET: api_key
pub fn verify_secret_t2_concat_real_fail() -> Bool {
    let api_key = "s3cret";
    let msg = "k=" + api_key;
    println(msg);
    return false;
}
