// # Print the secret spelling as a literal
//
// Logline: Bind and print a string whose value is only the secret spelling.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag api_key as secret.
//   2. Bind msg to the spelling "api_key".
//   3. Print msg.
//
// T2-1 FP: secret name only inside STRING literal — must NOT refuse
// SECRET: api_key
pub fn verify_secret_t2_string_fp() -> Bool {
    let msg = "api_key";
    println(msg);
    return true;
}
