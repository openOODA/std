// # Allow public process_exec argv
//
// Logline: Call process_exec with a public literal while a secret name sits unused.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag token as secret.
//   2. Call process_exec with literal "true".
//   3. Print 1 after the call.
//
// non-secret argv may process_exec (emit path)
// SECRET: token
pub fn verify_secret_process_exec_pass(p: &ProcessCap) -> Bool {
    let token = "s3cret-payload";
    let r = process_exec(p, "true");
    println(1);
    return true;
}
