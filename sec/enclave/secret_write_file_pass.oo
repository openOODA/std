// # Allow public write_file content
//
// Logline: Write a public string while an unused secret name sits nearby.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag token as secret.
//   2. Call write_file with public content.
//   3. Print 1 after the call.
//
// M128: non-secret content may write_file (emit path only; runtime still needs FsCap)
// SECRET: token
pub fn verify_secret_write_file_pass(fs: &FsWriteCap) -> Bool {
    let token = "s3cret";
    let public = "ok";
    let path = "/tmp/ooda_secret_sink_pass";
    let r = write_file(fs, path, public);
    println(1);
    return true;
}
