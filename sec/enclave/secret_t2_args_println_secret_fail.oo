// # Refuse a bare secret println
//
// Logline: Print a secret IDENT so the sink must refuse the call.
//
// Setup: Test fixture. Keep the SECRET tag and all code exact.
//
// Beats:
//   1. Tag token as secret.
//   2. Bind token to a string.
//   3. Print token.
//
// T2 peer1 args: println(secret) bare IDENT must refuse
// SECRET: token
pub fn verify_secret_t2_args_println_secret_fail() -> Bool {
    let token = "s3cret";
    println(token);
    return false;
}
