// # Merkle Facade
//
// Logline: Re-exports Merkle data types, operations, engine lifecycle, and fail-closed stubs.
//
// Setup: Pure module shim importing companion submodules.
//
// Beats:
//   1. Import Merkle companion modules.
//   2. Re-export public symbols with typed shim markers.
//   3. Maintain compatibility with downstream consumers.

import "../hash/sha256.oo";

type MerkleNode = struct {
    hash: Sha256State,
    left: Int,
    right: Int
};

// Residual API marker so the module typechecks; not a tree root.
pub fn dummy_merkle() -> Int {
    return 0;
}
