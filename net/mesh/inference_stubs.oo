// # Inference Engine Raw Weights Stub
//
// Logline: Fail-closed process_exit residual for raw-weight engine execution.
//
// Setup: Imports ./inference_ops.oo. oodac lacks raw pointer dereference and FFI.
//
// Beats:
//   1. inference_engine_run_raw_weights: refuse raw-weight dispatch.
//   2. process_exit(1): default-deny on unlowered hardware intrinsics.
//   3. return 0: keep the type signature callable from the shim.
//
// residual: not real inference; no tensor evaluator.

import "./inference_ops.oo";

// -------------------------------------------------------------------
// Fail-closed Boilerplate & Hardware Builtins Stubs
// -------------------------------------------------------------------

pub fn inference_engine_run_raw_weights(engine: &InferenceEngine, weights_ptr: Int, len: Int) -> Int {
    // COMPILER LIMITATION: Lack of language features like raw pointer memory dereferencing / low-level binary FFI.
    process_exit(1);
    return 0;
}
