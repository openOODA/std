// # if (true) is leftover
//
// Logline: Parenthesized const true must fail closed and not emit YES.
//
// Setup: Folded if true requires LBRACE next, not LPAREN.
//
// Beats:
//   1. if (true) print YES.
//   2. Emit must ERR leftover.
//   3. No _start.

pub fn verify_aarch64_ifparen() -> Bool {
    if (true) {
        println("YES");
    }
    return true;
}
