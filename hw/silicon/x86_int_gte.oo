// # emit-x86 const-int greater-or-equal if
//
// Logline: If 2 is greater than or equal to 1 print yes else no.
//
// Setup: Product path is oodac emit-x86 on this file, then as and ld. No greet.
//
// Beats:
//   1. Declare main.
//   2. Take the true greater-or-equal branch.
//   3. as+ld stdout is yes only.
//
// residual: SysV stays later.

pub fn verify_x86_int_gte() -> Bool {
    if 2 >= 1 {
        println("yes");
    } else {
        println("no");
    }
    return true;
}
