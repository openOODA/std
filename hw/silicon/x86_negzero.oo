// # emit-x86 unary-minus zero
//
// Logline: Print unary-minus 0 so emit-x86 can lower -0 to the same decimal as neg_dec.
//
// Setup: Product path is oodac emit-x86 on this file, then as and ld. No greet.
//
// Beats:
//   1. Declare main.
//   2. Print unary-minus 0.
//   3. as+ld stdout is 0, not -0.
//
// residual: ALU stays later.

pub fn verify_x86_negzero() -> Bool {
    println(-0);
    return true;
}
