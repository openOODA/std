// # emit-x86 else-less if false leftover
//
// Logline: If false print yes so emit-x86 must not invent a write.
//
// Setup: Product path is oodac emit-x86 on this file. No as+ld success.
//
// Beats:
//   1. Declare main.
//   2. Skip the then branch.
//   3. emit-x86 prints ERR x86_emit and no _start.
//
// residual: SysV stays later.

pub fn verify_x86_if_false() -> Bool {
    if false {
        println("yes");
    }
    return true;
}
