// # Const int println
//
// Logline: Print a const int literal and a same-fn int bind.
//
// Setup: Side emit-aarch64 writes decimal text, not ALU.
//
// Beats:
//   1. Print 42.
//   2. Bind n to 7.
//   3. Print n.

pub fn verify_aarch64_int() -> Bool {
    println(42);
    let n = 7;
    println(n);
    return true;
}
