// # UTF-8 println write length
//
// Logline: Print one U+00E9 so SYS_write length is UTF-8 bytes plus newline.
//
// Setup: Side emit-aarch64 must use bytes_len, not chars_len. é is two bytes.
//
// Beats:
//   1. Declare main.
//   2. Print é.
//   3. Expect mov x2, #3 after assemble-facing .ascii.

pub fn verify_aarch64_utf8() -> Bool {
    println("é");
    return true;
}
