// # emit-x86 mixed ident concat refuse
//
// Logline: Print a string plus an unbound ident so emit-x86 must fail closed.
//
// Setup: Fold misses. name is not a string-literal let.
//
// Beats:
//   1. Declare main.
//   2. Concat a literal and an ident.
//   3. emit-x86 must print ERR x86_emit.
//
// residual: mixed ident concat is not lowered.

pub fn verify_x86_mixed_plus() -> Bool {
    println("aa" + name);
    return true;
}
