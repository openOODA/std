// # if 1 > x is leftover
//
// Logline: INT GT IDENT must fail closed and not emit YES.
//
// Setup: x is a bind, not a const INT cond rhs.
//
// Beats:
//   1. Bind x to 0.
//   2. if 1 > x print YES.
//   3. Emit must ERR leftover.

pub fn verify_aarch64_ifgt_ident() -> Bool {
    let x = 0;
    if 1 > x {
        println("YES");
    }
    return true;
}
