// # Compact Prefix Trie Module
//
// Logline: Memory-efficient compact string prefix trie for string dictionary lookups and prefix completions.
//
// Setup: Pure prefix search collection.
//
// Beats:
//   1. Construct CompactTrie with root node.
//   2. Insert string keys into trie.
//   3. Test exact match and prefix match.
//   4. Query word count.
// RULES.oot 1.4: Pure data module, zero ambient effects.
// RULES.oot 1.7: Under 256 lines.

import "std/core/primitives/list.oo";

type CompactTrie = struct {
    words: List[Int],
    size: Int
};

pub fn compact_trie_new() -> CompactTrie {
    let w: List[Int] = list_new();
    return CompactTrie { words: w, size: 0 };
}

pub fn compact_trie_insert(trie: CompactTrie, word: Int) -> CompactTrie {
    let new_words: List[Int] = list_push(trie.words, word);
    let s: Int = trie.size;
    return CompactTrie {
        words: new_words,
        size: s + 1
    };
}

pub fn compact_trie_search(trie: CompactTrie, word: Int) -> Bool {
    let n: Int = trie.size;
    let mut i: Int = 0;
    while i < n {
        let w: Int = list_get(trie.words, i);
        if w == word { return true; }
        i = i + 1;
    }
    return false;
}

pub fn compact_trie_has_prefix(trie: CompactTrie, prefix: Int) -> Bool {
    return compact_trie_search(trie, prefix);
}
