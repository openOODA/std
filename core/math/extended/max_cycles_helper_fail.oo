// # Max Cycles Helper Fail
//
// Logline: Standard library verification and domain capabilities for Max Cycles Helper Fail.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

fn burn(n: Int) -> Int {
    let mut i = 0;
    while i < n {
        i = i + 1;
    }
    return i;
}
pub fn verify_max_cycles_helper_fail() -> Bool {
    println(burn(10));
    return true;
}
