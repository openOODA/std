// # List Ret Only
//
// Logline: Standard library verification and domain capabilities for List Ret Only.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

pub fn empty() -> List[String] {
    return list_new();
}
pub fn verify_list_ret_only() -> Bool {
    let x = empty();
    println(list_len(x));
    return true;
}
