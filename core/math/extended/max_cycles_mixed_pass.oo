// # Max Cycles Mixed Pass
//
// Logline: Standard library verification and domain capabilities for Max Cycles Mixed Pass.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

pub fn verify_max_cycles_mixed_pass() -> Bool {
    let mut a = 0;
    while a < 2 {
        a = a + 1;
    }
    let mut s = 0;
    for i in 0..2 {
        s = s + 1;
    }
    println(a + s);
    return true;
}
