// # Max Cycles Recursion Fail
//
// Logline: Standard library verification and domain capabilities for Max Cycles Recursion Fail.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

fn burn(n: Int) -> Int {
    if n <= 0 {
        return 0;
    }
    return burn(n - 1);
}

pub fn verify_max_cycles_recursion_fail() -> Bool {
    let x = burn(20);
    println(x);
    return true;
}
