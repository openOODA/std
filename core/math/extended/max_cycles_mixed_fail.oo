// # Max Cycles Mixed Fail
//
// Logline: Standard library verification and domain capabilities for Max Cycles Mixed Fail.
//
// Setup: Pure .oo standard library module.
//
// Beats:
//   1. Execute domain operations.
//   2. Verify invariants and return typed results.

pub fn verify_max_cycles_mixed_fail() -> Bool {
    let mut a = 0;
    while a < 2 {
        a = a + 1;
    }
    let mut s = 0;
    for i in 0..5 {
        s = s + 1;
    }
    println(s);
    return true;
}
