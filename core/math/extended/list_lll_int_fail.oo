// # List[List[List[Int]]] emit honesty lock
//
// Logline: emit-c fail-closes three-deep List rather than inventing OoLLL or OoLL_List.
//
// Setup: Backend-C. Two-deep List[List[Int]] stays product.
//
// Beats:
//   1. Annotate a List[List[List[Int]]].
//   2. Emit must not invent OoLLL_ or OoLL_List.
//   3. Fail closed with ERR c_emit.

pub fn verify_list_lll_int_fail() -> Bool {
    let xs: List[List[List[Int]]] = list_new();
    println("should_not_emit");
    return true;
}
