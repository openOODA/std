// # 3D Gaussian Operations & Transforms
//
// Logline: Destubbed onto ./gaussian_types.oo Int-floor API.
//
// Setup: Companion types destubbed; keep fail-closed real calls.
//
// Beats:
//   1. Import destubbed companion.
//   2. Wrap constructor and Int query.
//   3. Deterministic work helper for prove.

import "./gaussian_types.oo";

pub fn gaussian_ops_init(cx: Int, cy: Int, cz: Int, opacity: Int) -> Gaussian3D {
    return gaussian3d_new(cx, cy, cz, opacity);
}
