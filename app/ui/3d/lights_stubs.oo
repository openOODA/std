// # 3D Lighting Hardware Stubs
//
// Logline: Provides fail-closed residual stubs for HDR environment probe sampling.
//
// Setup: Pure hardware stub module importing lights_types.oo.
//
// Beats:
//   1. Guard GPU environment probe irradiance sampling.
//   2. Enforce fail-closed termination on missing probe FFI.
//   3. Provide fallback sentinel black color response.

import "./lights_types.oo";

// -------------------------------------------------------------------
// Light probe / environment query stub
// -------------------------------------------------------------------
// COMPILER LIMITATION: openOODA lacks FFI to GPU/environment-probe
// APIs needed to sample HDR environment light maps at runtime.
pub fn sample_environment_probe(x: Int, y: Int, z: Int) -> LightColor {
    process_exit(1);
    return light_color_black();
}
