// # Attitude Dynamics & Determination Facade
//
// Logline: implemented Int att_vec3 facade. Off Float shims and fail-closed stubs.
//
// Setup: Re-exports implemented attitude_types only. No List[Float]. No process_exit.
//
// Beats:
//   1. Construct AttVec3 via att_vec3_new.
//   2. Read x and y through facade getters.
// residual: ops/dcm/engine/stubs/Float shims implemented; this is the Int vec floor.

import "./attitude_types.oo";

pub fn attitude_vec3_new(x: Int, y: Int, z: Int) -> AttVec3 {
    return att_vec3_new(x, y, z);
}

pub fn attitude_vec3_x(v: AttVec3) -> Int {
    return att_vec3_x(v);
}

pub fn attitude_vec3_y(v: AttVec3) -> Int {
    return att_vec3_y(v);
}

pub fn attitude_vec3_z(v: AttVec3) -> Int {
    return att_vec3_z(v);
}
