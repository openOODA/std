// # ONNX graph node
//
// Logline: Integer ONNX node with operator id and input count.
//
// Setup: Pure .oo standard library module or subsystem.
//
// Beats:
//   1. Execute core data structures and functional algorithms.
//   2. Enforce invariant constraints and fail-closed Result returns.
//   3. Export domain interfaces for high-integrity composition.
//
// Implementation: pure discrete integer representations.

import "std/app/ai/ml/tensor_types.oo";

fn tensor_types_marker() -> Int { return dtype_float32(); }

type OnnxNode = struct {
    op_id: Int,
    inputs: Int
};

pub fn onnx_node_new(op_id: Int, inputs: Int) -> OnnxNode {
    return OnnxNode {
        op_id: op_id,
        inputs: inputs
    };
}

pub fn onnx_node_op_id(n: OnnxNode) -> Int {
    return n.op_id;
}
