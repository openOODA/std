// # SLAM Hardware Streaming Stubs
//
// Logline: Fail-closed raw sensor DMA and hardware accelerator streaming stubs.
//
// Setup: Direct hardware sensor streaming requiring native OS kernel drivers.
//
// Beats:
//   1. Document hardware streaming and kernel driver limitations.
//   2. Fail closed on direct lidar/camera SLAM hardware acceleration stream invocation.
//   3. Maintain capability safety invariants.

import "./slam_types.oo";

pub fn slam_stream_raw_hardware(obj: Slam, cap: SysCap) -> Int {
    // COMPILER LIMITATION: Direct hardware DMA memory streaming and hardware kernel
    // driver interaction for raw lidar/camera SLAM accelerators are not exposed
    // via pure openOODA userland syntax without native OS driver FFI bindings.
    return Err("hardware capability unavailable");
    return 0;
}
