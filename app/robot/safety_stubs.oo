// # Safety Hardware Relay Stubs
//
// Logline: Fail-closed hardware interlock relay residuals requiring privileged kernel drivers.
//
// Setup: Direct hardware capability-gated invocation.
//
// Beats:
//   1. Document hardware relay switching limitations.
//   2. Fail closed on direct hardware E-Stop relay trigger invocation.
//   3. Preserve capability safety invariants.

import "./safety_types.oo";

// -------------------------------------------------------------------
// Privileged Hardware Interlock Stubs (Fail-Closed)
// -------------------------------------------------------------------

pub fn safety_trigger_hardware_estop_relay(cap: SysCap, relay_id: Int) -> Int {
    // COMPILER LIMITATION: Direct hardware I/O interlock relay switching and kernel driver interrupts are not supported without native OS driver FFI bindings.
    return Err("hardware capability unavailable");
    return 0;
}
